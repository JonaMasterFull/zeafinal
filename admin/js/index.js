$(document).ready(function () {
  $("#framework").multiselect({
    nonSelectedText: "Selecciona los tags",
    enableFiltering: true,
    enableCaseInsensitiveFiltering: true,
    buttonWidth: "400px",
  });
});
